This notebook details the HackBio Building AI Agents for Biology Internship - Stage 0 project. It covers two main tasks:

    BASH Basic Scripting: This section demonstrates fundamental BASH commands, including creating directories, downloading files (wget), moving and deleting files (mv, rm), copying files (cp), checking for patterns in files (grep), and extracting specific information from files (awk). It also includes confirming if a .fna file is 'Mutant' or 'Wild Type' based on a pattern search.

    Bioinformatics Tool Installation: This part focuses on setting up a conda environment named 'funtools' and installing various bioinformatics tools such as bwa, blast, samtools, bedtools, spades, bcftools, and fastp. It also addresses an issue encountered during MultiQC installation by creating a separate conda environment with a specific Python version.

Finally, there's a section for Professional Profile linking to the user's GitHub repository and LinkedIn video related to this project.
